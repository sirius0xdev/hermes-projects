apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: landscaping-business-healthcheck
  namespace: landscaping-business
spec:
  targetRef:
    group: ""
    kind: Service
    name: landscaping-business-svc
  default:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 2
    unhealthyThreshold: 3
    config:
      type: HTTP
      httpHealthCheck:
        portSpecification: USE_SERVING_PORT
        requestPath: /
