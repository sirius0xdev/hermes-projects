apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: customer1
  labels:
    app: data-service
    app.kubernetes.io/name: data-service
    app.kubernetes.io/component: microservice
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    app: data-service
