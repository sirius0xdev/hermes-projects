"""Test configuration for data infrastructure tests."""
import sys
import os

# Ensure trading-platform root is on sys.path for imports
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))
